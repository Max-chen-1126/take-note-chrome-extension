# OS
.DS_Store

# Python (backend)
__pycache__/
*.py[cod]
.pytest_cache/

# uv / venv
.venv/

# Node / WXT (extension)
node_modules
.output
.wxt

# Package manager: repo standardizes on npm (package-lock.json committed).
# pnpm artifacts (from ad-hoc pnpm use) are local-only — ignore to avoid
# dual-lockfile drift. Switch fully to pnpm later if desired.
pnpm-lock.yaml
pnpm-workspace.yaml

# env
.env
.env.local
*.log
